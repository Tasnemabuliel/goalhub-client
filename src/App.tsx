import { Outlet } from 'react-router-dom'
import Navbar from './components/Navbar'

export default function App() {
  return <div style={{color:'green', fontSize:40, textAlign:'center', marginTop:100}}>בדיקת APP</div>;
}
